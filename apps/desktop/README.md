# Agent Engine Desktop Shell

Tauri v2 + React/TypeScript native desktop application for Agent Engine.

## Structure
- `src-tauri/`: Rust backend, system tray, sidecar process supervisor, OS keychain access.
- `src/renderer/`: React + TypeScript frontend (Kimi-style chat, OpenClaw-grade control dashboard, settings).
