# Agent Engine Python Sidecar Service

FastAPI + FastMCP + ACP local service running on port 8765, serving the local API contract (`packages/shared-schema/openapi.yaml`) to the Tauri desktop shell and IDE extensions.
